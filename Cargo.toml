[package]
name = "cutcut"
version = "0.2.1"
edition = "2024"

[dependencies]
glow = { package = "charmed-glow", version = "0.2.0" }
regex = "1"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
incremental = false
