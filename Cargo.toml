[package]
name = "cutcut"
version = "0.2.1"
edition = "2024"

[dependencies]
regex = "1"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
incremental = false
